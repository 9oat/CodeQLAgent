# Claude API 설정
# .env 파일로 복사해서 사용하세요: cp .env.example .env
# .env 파일은 gitignore에 포함되어 있어 커밋되지 않습니다.

CLAUDE_API_KEY=your_api_key_here

# CodeQL CLI 경로 (선택사항, 시스템 PATH에 있으면 생략 가능)
# CODEQL_PATH=/path/to/codeql

# 로그 레벨 (선택사항)
# RUST_LOG=info
